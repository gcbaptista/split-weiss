if ! pnpm format:check; then
  echo "\n❌ Formatting check failed. Run 'pnpm format' to fix it.\n"
  exit 1
fi

if ! pnpm lint; then
  echo "\n❌ Linting failed. Run 'pnpm lint -- --fix' to auto-fix issues.\n"
  exit 1
fi
